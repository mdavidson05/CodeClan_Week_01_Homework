from src.pet_shop import *
from tests.pet_shop_test import *

if __name__ == '__main__': #this is not necessary on newer versions of python
    unittest.main()

#when running through terminal you must have the run_tests.py must be highlighted otherwise it will not work
#if problem "no file called src" error it's because the wrong folder is highlighed in VSC